# K.A.N.A. Backend Environment Variables

# Database Configuration (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Google Gemini AI API Key (required for KANA backend)
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Override Gemini model used for QUIZ generation only
# Defaults to gemini-1.5-flash if not set
KANA_GEMINI_QUIZ_MODEL=gemini-1.5-flash

# Optional: Base Gemini model for general K.A.N.A. features (chat/OCR/etc.)
# Defaults to gemini-1.5-flash if not set
KANA_GEMINI_BASE_MODEL=gemini-1.5-flash

# Core API Key (if needed)
CORE_API_KEY=your_core_api_key_here

# Server Configuration
PORT=10000
NODE_ENV=production
